run `sudo bash install.sh` from inside the startup directory while it is housed in your home directory

.vimrc and .Xresources go in ~/

config and i3blocks.conf go in ~/.config/i3/


https://code.visualstudio.com/download

run `sudo dpkg -i ./vscodefilename.deb` to install vscode
